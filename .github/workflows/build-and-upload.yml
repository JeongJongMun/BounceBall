name: Build & Upload to Google Drive

on:
  push:
    branches: [main, dev]

jobs:
  build:
    name: Build StandaloneWindows64
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Cache Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-StandaloneWindows64-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-StandaloneWindows64-
            Library-

      - name: Build with Unity
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
          UNITY_EMAIL: ${{ secrets.UNITY_EMAIL }}
          UNITY_PASSWORD: ${{ secrets.UNITY_PASSWORD }}
        with:
          targetPlatform: StandaloneWindows64
          buildName: BounceBall

      - name: Zip build
        run: |
          sudo chown -R $USER build
          cd build/StandaloneWindows64
          zip -r "$GITHUB_WORKSPACE/BounceBall-${{ github.ref_name }}-${{ github.run_number }}.zip" .

      - name: Upload artifact (backup)
        uses: actions/upload-artifact@v4
        with:
          name: BounceBall-${{ github.ref_name }}-${{ github.run_number }}
          path: BounceBall-${{ github.ref_name }}-${{ github.run_number }}.zip
          retention-days: 7

      - name: Upload to Google Drive
        uses: adityak74/google-drive-upload-git-action@main
        with:
          credentials: ${{ secrets.GDRIVE_CREDENTIALS }}
          filename: BounceBall-${{ github.ref_name }}-${{ github.run_number }}.zip
          folderId: ${{ secrets.GDRIVE_FOLDER_ID }}
          overwrite: "true"
